import React from "react";

import Layout from "../components/Layout";

const Blog = () => {
  return (
    <Layout>
      <h1>
        This example utilises API routes for benchmarking - that's why there is
        nothing to show on the page
      </h1>
    </Layout>
  );
};

export default Blog;
